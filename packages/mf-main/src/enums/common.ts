/**
 * File type enum.
 */
export enum EFileType {
  JSON = 'json',
  XML = 'xml'
}

/**
 * Table sort columns enum.
 */
export enum ESortColumn {
  TEST_CASE_AMOUNT = 'testCaseAmount',
  ID = 'id'
}
